import axios from 'axios';

import {
    combineReducers
} from 'redux';

const List = (state={}, action) => {
    switch(action.type) {
        case getList:
            
        default:
            return state;
    }
}

export default combineReducers({List});